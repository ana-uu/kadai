<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Amazonランキング</title>
</head>
<body>
    <h1>Amazon売れ行きランキング</h1>
    <table border="1">
        <tr>
            <th>ランク</th>
            <th>商品名</th>
        </tr>
        <% data.forEach(function(item) { %>
        <tr>
            <td><%= item.rank %>位</td>
            <td><a href="/<%= item.id %>"><%= item.productName %></a></td>
        </tr>
        <% }); %>
    </table>
    <p><a href="/create">新規登録</a></p>
</body>
</html>