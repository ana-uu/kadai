<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= item.title %>の詳細</title>
</head>
<body>
    <h1><%= item.title %>の詳細</h1>
    <table border="1">
        <tr>
            <th>項目</th>
            <th>データ</th>
        </tr>
        <tr>
            <td>ランク</td>
            <td><%= item.rank %>位</td>
        </tr>
        <tr>
            <td>動画名</td>
            <td><%= item.title %></td>
        </tr>
        <tr>
            <td>ジャンル</td>
            <td><%= item.genre %></td>
        </tr>
        <tr>
            <td>価格</td>
            <td>¥<%= item.price %></td>
        </tr>
        <tr>
            <td>発売日</td>
            <td><%= item.releaseDate %></td>
        </tr>
        <tr>
            <td>女優名</td>
            <td><%= item.actress %></td>
        </tr>
        <tr>
            <td>リンク</td>
            <td><a href="<%= item.link %>" target="_blank"><%= item.link %></a></td>
        </tr>
    </table>
    <p><a href="/fanza/edit/<%= item.id %>">編集</a> | 
    <form action="/fanza/delete/<%= item.id %>" method="POST" style="display: inline;">
        <input type="submit" value="削除" onclick="return confirm('本当に削除しますか?')">
    </form></p>
    <p><a href="/fanza">ランキング一覧に戻る</a></p>
</body>
</html>